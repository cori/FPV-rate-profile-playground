{
  "name": "fpv-rate-profile-playground",
  "version": "1.0.0",
  "description": "Compare and visualize Betaflight rate profiles side-by-side",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.0.4",
    "vitest": "^1.0.4"
  }
}
